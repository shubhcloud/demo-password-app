apiVersion: v1
kind: Service
metadata:
  name: {{ include "password-app.fullname" . }}
  labels:
    {{- include "password-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "password-app.selectorLabels" . | nindent 4 }}
